sudo: required
services:
  - docker

before_install:
  - docker build -t web/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true web/react-test npm run test

after_success:
  - docker build -t web/multi-client ./client
  - docker build -t web/multi-nginx ./nginx
  - docker build -t web/multi-server ./server
  - docker build -t web/multi-worker ./worker
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push web/multi-client
  - docker push web/multi-nginx
  - docker push web/multi-server
  - docker push web/multi-worker
